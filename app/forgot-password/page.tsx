"use client";import{useState}from"react";import PublicNav from"@/components/PublicNav";import{supabase}from"@/lib/supabase";export default function Forgot(){const[msg,setMsg]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const email=String(f.get("email")||"");const redirect=`${location.origin}/reset-password`;const{error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:redirect});setMsg(error?error.message:"Изпратихме линк, ако email-ът съществува.")}return <main className="page"><PublicNav/><section className="form-wrap"><div className="form-card"><div className="badge">RESET</div><h1>Забравена парола</h1><form onSubmit={submit}><div className="field"><label>Email</label><input name="email" type="email" required/></div><button className="btn btn-primary">Изпрати линк</button>{msg&&<p className="form-message">{msg}</p>}</form></div></section></main>}
