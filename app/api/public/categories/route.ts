import{NextResponse}from"next/server";import{getSupabaseAdmin}from"@/lib/supabaseAdmin";import{BEAUTY_CATEGORIES}from"@/lib/beautyflow/categories";export async function GET(){try{const db=getSupabaseAdmin();const{data,error}=await db.from("platform_categories").select("value,label").eq("active",true).order("sort_order");if(error)throw error;return NextResponse.json({categories:data||BEAUTY_CATEGORIES})}catch{return NextResponse.json({categories:BEAUTY_CATEGORIES})}}
